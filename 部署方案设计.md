# 志愿者服务管理系统 - 部署方案设计

本文档仅做**部署架构与方案设计**，不涉及多管理员账号的代码实现。目标是：**短期单机可在各系统上部署使用，中长期能平滑过渡到多机、多账户、中心库**。

---

## 一、需求概览

| 阶段 | 场景 | 要求 |
|------|------|------|
| **短期** | 单机使用 | 每台电脑（Win10 / Win7 / Windows XP）都能独立部署、独立使用，数据仅在本机 |
| **中长期** | 多机多账户 | 多台电脑用不同管理员账号登录，按部门分配账号，**所有数据集中在同一数据库**；部门管理员用各自 Win10/Win7/XP 电脑**远程管理** |
| **服务器** | 可选 | 您有一台 Win10 虚拟机可作为中心服务器 |

---

## 二、短期：单机部署方案（每台电脑独立运行）

单机模式下，每台电脑都运行「前端 + 后端 + 数据库」，数据存在本机，互不影响。

### 2.1 按操作系统划分

| 系统 | 推荐方式 | 说明 |
|------|----------|------|
| **Windows 10** | 方案 A（Docker 一键）或 方案 B（Node + MySQL） | Docker 仅支持 Win10/11，最省事；若不想用 Docker 则用方案 B |
| **Windows 7** | 方案 B（Node + MySQL） | 无法使用 Docker，需在本机安装 Node.js + MySQL，或后续做「绿色版」 |
| **Windows XP** | 方案 C（仅浏览器访问） | 本机不装 Node/数据库；通过浏览器访问**同一局域网内**某台已部署好的电脑（如某台 Win10 单机），或后续仅作中长期时的**瘦客户端** |

### 2.2 方案 A：Docker 一键（仅 Win10 / Win11，可选）

- **适用**：若项目提供 Docker Compose 与 `启动.bat`，可在 Win10/11 上一键运行。
- **优点**：无需单独安装 Node、MySQL，双击即可运行。
- **缺点**：仅支持 Win10/11，不支持 Win7/XP；当前主推方案 B，Docker 方案可按需补充。
- **使用**：复制整个项目 → 安装 Docker Desktop → 双击 `启动.bat`。

### 2.3 方案 B：Node.js + MySQL 单机部署（Win10 / Win7 统一方案，当前主推）

- **适用**：Win10（不想用 Docker 时）、Win7。
- **步骤**：详见 **`安装指南.txt`**，简要为：  
  1. 安装 [Node.js](https://nodejs.org/) LTS（安装时勾选 Add to PATH）。  
  2. 安装 [MySQL](https://dev.mysql.com/downloads/mysql/) 5.7 或 8.0，root 密码建议设为 `1234`，执行 `database/init.sql` 初始化库表。  
  3. 将项目拷到该电脑（或从 GitHub clone）后，把项目内的 **`env.default`** 复制为 **`.env`**（若该机 MySQL 密码不是 `1234`，用记事本打开 `.env` 修改 `MYSQL_PASSWORD` 即可，便于在多台电脑上分别配置）。  
  4. 双击 **`启动-Node单机.bat`**（首次运行会自动安装根目录与前端依赖），或手动执行 `npm install`、`cd frontend && npm install` 后再执行 `npm run dev`。  
  5. 浏览器访问 `http://localhost:5173`，默认管理员 admin / 1234。
- **优点**：同一套代码 Win10/Win7 都能跑；通过根目录 `.env` 配置数据库（项目提供 `env.default` 作为模板，拷到其他电脑后复制为 `.env` 即可录入该机密码）。  
- **缺点**：每台机器需安装 Node 与 MySQL。

**与中长期的关系**：单机时后端连接本机 MySQL；将来迁到中心库时，只需改 `.env` 中的 `MYSQL_HOST` 等指向服务器，无需改前端代码。

### 2.4 方案 C：XP 与「仅浏览器」的机器

- **短期**：  
  - XP 上**不部署**本系统（Node 对 XP 支持极差，且 Docker 不可用）。  
  - 若某台 Win10/Win7 已单机部署，XP 可通过浏览器访问该机的前端地址，例如：`http://192.168.1.100:5173`（前提：该机前端对局域网开放、防火墙放行 5173 与后端 3000）。  
- **中长期**：XP 仅作为**瘦客户端**，浏览器访问中心服务器地址即可，无需在本机安装任何服务。

### 2.5 短期小结

- **Win10**：可选 Docker 一键；当前主推 **Node + MySQL**，按 `安装指南.txt` 操作，拷贝或 clone 项目后复制 `env.default` 为 `.env` 并按需改密码，双击 `启动-Node单机.bat` 即可。  
- **Win7**：Node + MySQL（同一套步骤；或未来做绿色版/便携 MySQL）。  
- **XP**：不装服务端，用浏览器访问其他已部署机器的 `http://该机IP:5173` 或未来的中心服务器。  
这样即可实现「每台需部署的电脑（Win10/Win7）都能单机部署使用」，XP 通过访问其他机器参与使用。

---

## 三、中长期：多机多账户、中心数据库

### 3.1 架构示意

```
                    ┌─────────────────────────────────────┐
                    │  中心服务器（您的 Win10 虚拟机）       │
                    │  - Node 后端（当前项目 backend）      │
                    │  - MySQL（唯一数据库，所有数据集中）   │
                    │  - 前端静态（当前项目 frontend 构建） │
                    │  - 端口例：3000（或 80）             │
                    └─────────────────────────────────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │                   │
             部门A 管理员           部门B 管理员           部门C 管理员
             Win10/Win7/XP         Win10/Win7/XP         Win10/Win7/XP
             浏览器访问            浏览器访问            浏览器访问
             http://服务器IP:3000  http://服务器IP:3000  http://服务器IP:3000
             账号: dept_a / xxx    账号: dept_b / xxx    账号: dept_c / xxx
```

- **服务器**：只部署一份「前端 + 后端 + MySQL」，一个库存所有志愿者与服务记录。  
- **客户端**：各部门管理员用各自电脑（Win10/Win7/XP）的**浏览器**打开服务器地址，用**不同管理员账号**登录（按部门分配，代码层面后续再实现）。  
- **数据**：全部落在中心 MySQL，便于统一查询、统计与权限按部门划分。

### 3.2 服务器端部署（Win10 虚拟机）

- **方式 1（推荐）**：在虚拟机上用现有 **Docker 方案**  
  - 复制项目到虚拟机 → 安装 Docker Desktop → 双击 `启动.bat`（或 `docker compose up -d`）。  
  - 将后端/前端对外端口（如 3000）对**局域网或公网**开放（防火墙、端口映射或域名反向代理）。

- **方式 2**：在虚拟机上 **Node + MySQL**  
  - 安装 Node、MySQL，执行 `database/init.sql`，用 `npm run dev` 或构建后 `node backend/app.js` + 静态托管。  
  - 同样需开放防火墙。后端默认监听所有网卡（`listen(3000)` 即可被外网访问），无需改代码。

- **要求**：服务器 IP 固定或使用域名，便于各部门电脑浏览器访问；防火墙放行相应端口（如 3000、5173 或 80）。

### 3.3 客户端（各部门管理员电脑）

- **无需在本机安装** Node、MySQL、Docker。  
- 只需**浏览器**（Chrome / Edge / Firefox；XP 可用兼容的浏览器）访问：  
  `http://服务器IP或域名:3000`  
- 登录后使用各自分配的管理员账号（多账户、按部门等功能后续在代码中实现）。

### 3.4 多管理员账号（仅设计，不在此实现代码）

- **目标**：不同部门对应不同管理员账号，登录后只能操作本部门或约定范围内的志愿者数据（具体权限由后续需求定）。  
- **实现预留**：  
  - 数据库已有 `admin` 表，可扩展字段（如 `department`、`role`）。  
  - 后端登录接口与权限中间件后续可基于「部门/角色」做校验。  
  - 前端根据登录身份做菜单/范围限制。  
- 当前阶段**仅做部署设计**，不在本方案中改登录与权限代码。

---

## 四、从短期到长期的迁移路径

### 4.1 数据与配置

- **短期**：每台单机一个 MySQL，数据分散在各机。  
- **迁移时**：  
  - 在中心服务器上部署好「后端 + MySQL + 前端」，并执行 `database/init.sql`（若库已存在则注意不要覆盖已有数据）。  
  - 将各单机的 MySQL 数据**导出**（mysqldump 或按表导出），经去重、清洗后**导入**到中心 MySQL。  
  - 管理员账号在中心库中统一创建/迁移（含后续部门、角色等字段）。

### 4.2 各单机向「仅客户端」切换

- 单机不再运行本机 Node + MySQL（或 Docker）。  
- 在本机浏览器中把访问地址从 `http://localhost:5173` 改为 `http://中心服务器IP或域名:3000`。  
- 无需重装或改项目代码，只改「访问的地址」和「登录的账号」。

### 4.3 兼容性

- 当前项目**同一套代码**可同时用于：  
  - **短期**：单机部署（Docker 或 Node+MySQL），连接本机 MySQL。  
  - **长期**：服务器部署同一套，连接中心 MySQL；客户端仅用浏览器。  
- 后端已支持通过环境变量配置数据库（`MYSQL_HOST` 等），将来在服务器上设好环境变量即可指向中心库，无需为「单机/多机」维护两套代码。

---

## 五、方案对照与建议

| 维度 | 短期（单机） | 中长期（多机多账户） |
|------|--------------|----------------------|
| **部署位置** | 每台需使用的电脑（Win10/Win7） | 仅一台：您的 Win10 虚拟机 |
| **Win10** | Docker 一键 或 Node+MySQL | 仅浏览器访问服务器 |
| **Win7** | Node+MySQL | 仅浏览器访问服务器 |
| **XP** | 不部署；浏览器访问同网段某台单机 | 仅浏览器访问服务器 |
| **数据库** | 每机一个 MySQL（或未来绿色版） | 中心一个 MySQL |
| **管理员账号** | 当前单账号 admin | 后续多账号、按部门（代码另做） |

**建议**：  
- **短期**：Win10/Win7 统一用 **Node + MySQL**（按 `安装指南.txt`，拷贝或 clone 项目 → 复制 `env.default` 为 `.env` 并按需改密码 → 双击 `启动-Node单机.bat`）；若需 Win10 Docker 一键可再补；XP 统一用浏览器访问已部署机器或未来中心服务器。  
- **中长期**：在 Win10 虚拟机上用 Docker 或 Node+MySQL 部署一份中心服务，对外提供 `http://服务器:3000`（或 80），各部门用浏览器 + 后续实现的多账号登录即可。  
这样即可在**不提前改多账户代码**的前提下，保证短期单机与中长期多机、多账户、中心库都能按同一套部署思路顺利落地。

---

## 六、文档与脚本（当前情况）

- **短期单机**：已提供 **`安装指南.txt`**（简明五步，适用 Win7/Win10/Win11）；**`启动-Node单机.bat`** 首次运行会自动安装根目录与前端依赖；**`env.default`** 作为数据库配置模板，拷到或 clone 到其他电脑后复制为 `.env` 即可录入该机 MySQL 密码。  
- 建议在 **README** 中增加「部署方案总览」并引用本文档（`部署方案设计.md`）。  
- **中长期**：服务器部署时可单独写一份「中心服务器部署检查清单」（端口、防火墙、`.env` 中 `MYSQL_HOST` 等），便于一次性配置正确。

以上为部署方案设计，未对多管理员账号做任何代码修改，仅保证短期单机与中长期多机多账户的部署路径清晰、可执行。
